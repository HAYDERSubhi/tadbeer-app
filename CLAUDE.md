# تدبير — دليل المشروع (Source of Truth)

> هذا الملف يُحمَّل تلقائياً في بداية كل جلسة. الهدف: ألّا يعتمد Claude على الذاكرة أو المحادثات، بل على الكود الفعلي.

## ⛔ القاعدة الإلزامية الأولى

**الكود الموجود على القرص هو المصدر الوحيد للحقيقة.** المحادثات السابقة والافتراضات والذاكرة كلها معلومات مساعدة قد تكون قديمة.

- **ممنوع** تأكيد وجود أو غياب أي ميزة قبل قراءة ملفها الفعلي. عبارة "غير موجود" ممنوعة قبل البحث.
- قبل أي تعديل: حدّد الملفات المتأثرة، اقرأها **كاملةً**، افحص أماكن استدعائها، ثم اقترح قبل التنفيذ.
- لا تنشئ ملفاً/دالة/مزوّداً جديداً إذا وُجد بديل يؤدي نفس الوظيفة — أعد الاستخدام وحافظ على النمط المعماري.
- عند تعارض المحادثة مع الكود: الكود هو المرجع الرسمي.
- إن كان ملف مفقوداً أو غير واضح: توقّف واطلبه، لا تخمّن.

## ⛔ جلسة واحدة تكتب — والبقية تقرأ

**الخطر:** كل جلسة تحتفظ في ذاكرتها بنسخة من الملفات التي قرأتها. فإن كتبت جلسةٌ في
ملف بعد أن قرأته جلسةٌ أخرى، ثم عدّلته الأخرى، **كتبت نسختها القديمة فوق الأحدث** —
فيُمحى العمل الجديد **بصمت**: بلا رسالة تعارض، وبلا تحذير، وحالة `git` تبدو سليمة
تماماً. التعارض يصرخ فيُنتبه له، وهذا يمرّ بهدوء فلا يُكتشف إلا بعد أن يضرّ.

**القاعدة:**
- **جلسة واحدة فقط تكتب في هذا المجلد في الوقت نفسه.** البقية **قراءة فقط**.
- إن شككت أن جلسة أخرى تعمل الآن: **توقّف واسأل صاحب المشروع قبل أي كتابة.**
- ولو طُلب منك دمج عمل جلسة أخرى: لا تنسخ من ذاكرتها. اطلب منها **تقريراً بلا كتابة**
  (ما الملفات؟ محفوظة أم لا؟)، ثم تحقّق أنت من القرص و`git log`.

**قبل أي كتابة في جلسة طالت أو انقطعت أو استُؤنفت — تأكّد أن نسختك حديثة:**
```bash
git log --oneline -3 && git status --porcelain
```
إن ظهر التزام لا تعرفه، أو تعديل لم تفعله أنت ⇒ **نسختك من الملفات قديمة.**
أعد قراءة كل ملف ستعدّله من القرص قبل تعديله، ولا تعتمد على ما تتذكّره منه.

> أخطر الملفات هنا لأن جلسات كثيرة تلمسها: `CLAUDE.md` · `firestore.rules` ·
> `src/services/firestore.ts` · `src/app/(main)/page.tsx`. أعد قراءتها دائماً قبل التعديل.
>
> حدث هذا فعلاً 2026-09-05: جلستان عملتا على المشروع في اليوم نفسه (١١ التزاماً ثم ٩).
> نجا العمل **لأنهما لم تتزامنا** لا لأن هناك ما يمنع التصادم — والثانية تحقّقت بعدها
> أن عمل الأولى سليم بمقارنة `git diff` لا بالثقة. وهذه القاعدة كُتبت كي لا تُترك النجاة للصدفة.

## 📍 الوضع الحالي — ⛔ اقرأه كاملاً قبل **أي** عمل، لا الأمني والبنيوي فقط
> فيه ما أُنجز فعلاً (فلا يُعاد)، وما حُسم بقرار (فلا يُناقَش من جديد)، وما أُجِّل
> عمداً (فلا يُبدأ بلا إذن)، وفخاخاً تبدو إصلاحات سهلة وهي تكسر.
> **العودة إلى شيء هنا بلا سبب جديد = إعادة عمل مدفوع الثمن مرّتين.**
> آخر تحديث: **2026-09-09** · النسخة المنشورة وقتها: `0326dad`
> (نُشرت ومُتحقَّق منها على الإنتاج: `/landing` واللقطات ⇒ ٤٠٤ · الشاشات العامة
> الخمس ⇒ ٢٠٠ · عامل الخدمة المنشور بلا أثر لهما و`/~offline` ما زالت محفوظة.)
> **مَن يعمل على أي بند هنا، يُحدِّث هذا القسم قبل أن يُنهي عمله.**

### ✅ أُصلح ونُشر ومُتحقَّق منه على الإنتاج — لا تُعِد عمله
| البند | أين | الحالة على الإنتاج |
|---|---|---|
| مصادقة خدمات الذكاء الخمس | `lib/api-auth.ts` + `lib/api-fetch.ts` | ٤٠١ بلا رمز · ٥٠٣ لعطل الإعداد |
| قواعد العائلة (منع الاستيلاء/الإتلاف) | `firestore.rules` | ٤٠٣ للهجوم · مغادرة العضو تعمل |
| قواعد الإحالات (منع التزوير) | `firestore.rules` | ٤٠٣ للتزوير وإحالة النفس |
| حذف الحساب: `trips` · `badges` · `pushSubscriptions` | `api/delete-account` | مُختبَر بحساب حقيقي |
| أسعار العملات الناقصة/التالفة | `use-exchange-rates.ts` | نسخة ناقصة تُرفض فعلاً |
| فحص الأنواع (استثناء `creative-studio`) | `tsconfig.json` | أخطاء `src/`: **١** فقط |
| التقرير كان **ينفي** مصاريف الشهور الأقدم من ٦ أشهر (والمشاركة تنشر «الإجمالي: ٠») | `report/page.tsx` + `endDate` في `getExpenses` | يجلب الشهر الأرشيفي عند الطلب · جرّبه المالك على هاتفه |
| جلب مزدوج لكل عضو عائلة بكل فتح بارد | `use-app-data.tsx` (`tadbeer-hh:<uid>`) | استعلام واحد بالمسار الصحيح |
| الرئيسية تعود «هيكلاً عظمياً» فوق بيانات ظاهرة عند كل إعادة جلب | `use-app-data.tsx` | المحتوى يبقى · التحديث بصمت |
| «ملخص الأسبوع الماضي» كان يظهر السبت **والأحد**، وزر «تم، شكراً» لا يُخفيه | `weekly-summary-card.tsx` | السبت موعده · الأحد لمن لم يفتح التطبيق السبت · مُتحقَّق من نزول النسخة |
| «شاركنا رأيك» لا يفتح شيئاً (الاستمارة داخل قسم مطويّ ⇒ غير مُركَّبة أصلاً) | `settings/page.tsx` | الاستمارة على مستوى الصفحة · «حول تدبير» صار آخر الشاشة |
| زر التقييم يفتح صفحة Play **على المتصفّح — وهي بلا أداة نجوم إطلاقاً** | `settings/page.tsx` (`handleRateClick`) | أندرويد ⇒ `intent://…;package=com.android.vending;end` · مُتحقَّق من وجوده في الحزمة المنشورة |
| التطبيق كان **يبعد أندرويد عن المتجر**: لا رابط Play في كل الكود، ولافتة تدفع للتثبيت من المتصفّح | `play-install-banner.tsx` · `play-store-badge.tsx` · `lib/platform.ts` | أندرويد ⇒ Google Play · القديمة اختفت من الحزمة المنشورة (مُتحقَّق) |
| **لا شاشة تعمل بلا إنترنت** عدا التي بدأ منها المستخدم — وزر الحفظ يتجمّد | `next.config.js` · `~offline/page.tsx` · `offline-indicator.tsx` · `add-expense/page.tsx` | `/~offline` تُخدم ٢٠٠ ومحفوظة مسبقاً · منطق حفظ الشاشات وصل الحزمة المنشورة |
| صفحة هبوط ميتة على القرص + ٧ لقطات يتيمة تُنزَّل على كل جهاز بكل نشر | حُذفت `app/landing/` · اللقطات إلى `docs/assets/screenshots/` | الحفظ المسبق **٧٫٧ ⇐ ٤٫٥٩ م.ب** (١٤١ ⇐ ١٣٢ ملفاً)، مقيس بعد البناء |

> ⛔ **لا تُبسِّط زر التقييم إلى `market://` المجرَّدة.** جُرِّبت ونُشرت 2026-09-08
> (`13a6b3b`) ففتحت على سامسونج نافذة «Open with: Galaxy Store / Google Play» —
> ومتجر Galaxy لا يحوي تدبير أصلاً، فمن يختاره يصل إلى لا شيء. الصيغة الحالية
> (`bb3fe92`) تقصد Google Play بالاسم عبر `package=com.android.vending` فلا نافذة
> اختيار. الـ `href` يبقى رابط ويب عمداً (سطح المكتب + «نسخ الرابط»).

> ⛔ **لا تُعِد التثبيت من متصفّح أندرويد (`beforeinstallprompt`).** أُزيل بقرار
> صريح لصاحب المشروع 2026-09-08 (`5690343`): «أي مستخدم أندرويد يُوجَّه إلى جوجل
> بلي». لافتة «أضف التطبيق لشاشتك الرئيسية» كانت تلتقط **أثمن لحظة تثبيت في
> المنتج** (بعد التسجيل، داخل التطبيق) وتوجّهها كلها إلى المتصفّح — ومن ثبّت منه
> **لا يسمح له Google بالتقييم إطلاقاً** ولا يُحتسب تنزيلاً. حُذف معها
> `install-banner.tsx` و`use-pwa-install.tsx`. **`IosInstallBanner` باقية عمداً**
> (لا Google Play على آيفون). شرط «أندرويد وغير مثبِّت» + رابط المتجر في مكان
> واحد: `lib/platform.ts` — استعمله ولا تكرّره.
>
> ⛔ **ولا تحذف `BrowserInstallSuppressor` من `providers.tsx`.** يبدو بلا وظيفة
> ظاهرة وهو ليس كذلك: يكتم نافذة «Install» التي يعرضها **كروم نفسه** لأي موقع
> مؤهَّل كـ PWA. كان هذا الكتم مدفوناً داخل `use-pwa-install` **ففُقد سهواً** مع
> حذفه في `5690343`، فظهر للمستخدم عرضان متناقضان: نافذة كروم أعلى الشاشة تدفعه
> للتثبيت من المتصفّح، ولافتتنا أسفلها تدعوه لجوجل بلي (رصدها المالك على هاتفه).
> أُعيد وحده في `cd22353`. **غير مشروط بمنصّة عمداً** — استعادةٌ للسلوك السابق
> حرفياً لا سلوك جديد على سطح المكتب.
>
> ⛔ **ولا تحاول إظهار دعوة تقييم «لمستخدمي المتجر وحدهم» — مستحيل، فُحص وسقط
> 2026-09-08.** لا يستطيع التطبيق تمييز نسخة Google Play عن المتصفّح: `startUrl`
> في `twa-manifest.json` مجرَّدة بلا علامة (ووضع علامة فيها يعني إصدار Play جديد
> مع مراجعة)، وبصمة `android-app://` **لا تصل** — بحث GA4 عن `android` في
> Session source/medium لم يُرجع شيئاً، وقناة Referral كلها ٤١ جلسة من ٨٬٣١٥.
> وحتى لو وصلت فهي بصمة الإقلاع البارد وحده وتُمحى بأي إعادة تحميل.
>
> ⚠️ **القرار اتُّخذ بلا أرقام، وهذا معروف:** اللافتة القديمة لم تكن مقيسة إطلاقاً.
> لذلك أُضيف القياس شرطاً: `play_banner_view` (المقام) · `play_banner_click` ·
> `play_banner_dismiss` · و`play_badge_view`/`play_badge_click` لصفحة التسجيل.
> **راجعها بعد 2026-09-22 وقارن تحويل التسجيل بخط الأساس ~18%؛ إن تراجع التثبيت
> بشدّة فالتراجع عن القرار مشروع.** ⛔ لا تقرأ الضغطات بلا مقامها.

### 📴 العمل بلا إنترنت — نُشر 2026-09-08 (`0cfcaf8`)
كان المستخدم يفصل الإنترنت فتعمل الشاشة التي هو فيها **وحدها**، وأي ضغطة تنقّل
تعطي شاشة كروم «This site can't be reached». السبب: `runtimeCaching` يحفظ طلبات
التنقّل (`request.mode === 'navigate'`) فقط، و**التنقّل عبر `next/link` ليس منها**
(يجلب حمولة RSC لا مستنداً)، و`cacheOnFrontEndNav` كان `false`.
> قياس على الإنتاج: فتح `/signup` ثم الضغط على رابط `/login` ⇒ الصفحة انتقلت فعلاً
> و`/login` **لم يدخل** `pages-cache`. ⇒ عملياً لا شاشة تُحفَظ إطلاقاً.

- ⛔ **لا تُعِد `cacheOnFrontEndNav` إلى `false`** — هو الإصلاح كلّه.
  `aggressiveFrontEndNavCaching` يبقى `false` عمداً (ملفاتنا الثابتة محفوظة مسبقاً).
- ⛔ **لا تُعِد تسمية `src/app/~offline/`.** المكتبة تبحث عن هذا الاسم حرفياً لموجّه
  App، و`_offline` **لا يصلح**: مجلدات `_` في App Router خاصة ولا تُنشئ مساراً —
  فتختفي الصفحة بصمت. (وهي خارج `(main)` عمداً: لا تعتمد على مصادقة ولا بيانات.)
- ⛔ **قائمة «يمكنك الآن» في الصفحة عهدٌ لا زينة.** كُتب فيها «تسجيل مصروف يدوياً»
  لأن تجمّد زر الحفظ أُصلح معها. أي تغيير يكسر بنداً يوجب حذفه من القائمة.
- الشريط البرتقالي: نصّه صار «أنت غير متصل — بياناتك محفوظة». ⛔ لا تُعِد «يمكنك
  الاستمرار وسيتزامن…» — كان وعداً كاذباً. وهو الآن **داخل** هيدر `AppShell` لا
  `fixed` فوقه (كان يقصّ الشعار والأيقونات — ظاهر في لقطة المالك).
- ⚠️ **لا تحكم على العمل بلا إنترنت مباشرةً بعد نشر.** كل نشر يغيّر بصمات كل
  الملفات، فيُعيد عامل الخدمة تنزيل **~٤٫٦ ميغابايت** (١٣٢ ملفاً) إلى كل جهاز.
  (كانت ٧٫٧ م.ب و١٤١ ملفاً حتى 2026-09-09 — نقصت بنقل لقطات الهبوط اليتيمة خارج
  `public`؛ القياس بجمع أحجام ما يسرده `public/sw.js` فعلاً، لا بالتقدير.)
  من يقطع الشبكة قبل اكتماله يرى أصولاً ناقصة — ظهر فعلاً 2026-09-08: صور
  «الأدوات المالية» بدت مكسورة، وكانت **كلها في قائمة الحفظ المسبق** أصلاً،
  ورجعت سليمة بعد ٣٠ ثانية اتصال. **شبح لا عطل — لا تطارده.**
  > 🔎 قياس جانبي يستحق قراراً: أيقونات الأدوات الثماني **٧٤٨ كيلوبايت**، مقاسها
  > ٣٧٥×٣٧٥ بكسل وتُعرض بـ٥٦ فقط (أثقلها silftna ١٥٦ ك.ب). تصغيرها يقلّص ما ينزل
  > على كل مستخدم بكل نشر. **عُرض على صاحب المشروع ولم يُقرَّر — لا تبدأه بلا إذنه.**
- ✅ **مُعايَن على هاتف المالك فعلاً (2026-09-08)** بعد النشر: فصل الشبكة، فُتحت
  الشاشات المزارة، وسُجّل مصروف وحُفظ **بلا تجمّد**، وظهرت صفحة `~offline` للشاشة
  غير المزارة، والشريط لم يعد يقصّ الهيدر. **هذا هو الاختبار الوحيد الحاسم** —
  ولا يُنجَز من جلسة Claude (انظر السطر التالي).
- ⚠️ **عامل الخدمة لا يُسجَّل في متصفّح جلسة Claude** («unknown error when fetching
  the script») ولا يعمل في `npm run dev` (`disable` على التطوير). فاختباره يحتاج
  `npm run build && npm start` + جهازاً حقيقياً. التحقق المتاح من الجلسة **ساكن**:
  سجلّ البناء (`Documents (pages): /~offline`) · وجود `/~offline` في `sw.js` ·
  توليد `swe-worker-*` ومعالجته `__FRONTEND_NAV_CACHE__` · ووجود الرمز في الحزمة.

⚠️ **قواعد Firestore لا تُنشر مع Vercel.** تحتاج: `firebase deploy --only firestore:rules`
(ورمز دخول firebase ينتهي دورياً ⇒ `firebase login --reauth` بطرفية صاحب المشروع).

### 🔐 محو الحساب الكامل — نُشر ومُتحقَّق منه 2026-09-06 (`96cf420`)
كان يمحو `users/{uid}` وحده، فمن كان في عائلة تبقى مصاريفه وأهدافه ودخله تحت
`households/{id}/`، ويبقى **اسمه وبريده** في `members` إلى الأبد أمام بقية
الأفراد، ولم يكن أي سطر في التطبيق يحذف من Storage. وهذا يخالف نصّ شاشة الحذف
(«حسابك وجميع بياناتك») ونصّ سياسة الخصوصية صراحةً.
- المنطق في `lib/account-erasure.ts` (مفصول عن المسار **عمداً** كي يُختبَر — فخّ ٢).
- المسار `api/delete-account` صار هوية + ترتيب فقط، و**أُضيف له `maxDuration = 60`**
  الذي كان ناقصاً: كان يعمل بمهلة ١٠ ثوانٍ وحده بين كل نظائره، فحذف مستخدم قديم
  كان ينقطع في منتصفه.
- قرارا صاحب المشروع (2026-09-06): مالك يحذف حسابه ⇒ **الملكية لأقدم عضو باقٍ**
  (لا مَنْع، ولا حلّ للعائلة) · ملاحظات «شاركنا رأيك» ⇒ **يبقى النصّ وتُمحى الهوية**.
- شاشة الحذف: تحذير أصفر يظهر **لمن هو في عائلة وحده** — الحذف ينقص المجموع
  المشترك الذي يراه الآخرون، فلا يصحّ أن يكتشفوه بعد وقوعه.
- ✅ `npm run test:erasure` — ٣٧ فحصاً على محاكي Firestore+Storage، خمس حالات
  (عضو · مالك ومعه آخرون · آخر عضو · بلا عائلة · إعادة تنفيذ). لا يمسّ الإنتاج.
  **شغّله قبل أي تعديل على `account-erasure.ts`.**
- ✅ مُتحقَّق على الإنتاج: `POST /api/delete-account` بلا رمز ⇒ **٤٠١** (أي أن
  المسار حمّل `firebase-admin/storage` بنجاح، لا ٥٠٠)، ونصّ التحذير موجود فعلاً
  داخل حزمة `settings` المنشورة.

- ✅ **التحذير مُعايَن على هاتف المالك فعلاً (2026-09-07، Galaxy):** النصّان
  ظاهران (العائلة + نقل الملكية)، الاتجاه سليم، وزر الحذف معطَّل حتى تُكتب كلمة
  التأكيد. لا يُعايَن من جلسة Claude — الشاشة خلف تسجيل الدخول.

⚠️ **مجهول واحد باقٍ — يحتاج أول حذف حساب حقيقي:**
هل لمفتاح خدمة الإنتاج صلاحية حذف من Storage؟ لو لا، **لا يُمنع المستخدم من
الحذف** — يُسجَّل الأثر في `deletionResidue/{uid}`. **افحص هذه المجموعة: أي
مستند فيها = صلاحية Storage ناقصة على مفتاح الخدمة.**

### ⏸️ مُؤجَّل بقرار صاحب المشروع — ليس سهواً، ولا تبدأه بلا إذنه
1. ~~حذف الحساب ناقص في الأهم~~ ⇒ **أُصلح ونُشر 2026-09-06** — انظر القسم أعلاه.
2. نسخة Firestore المحلية لا تُمسح عند تسجيل الخروج.
3. تداخل حسابين على جهاز واحد — **انظر «فخّ ١»، لا تُصلحه ساذجاً**.
4. رمز دعوة العائلة دائم بلا إلغاء ولا انتهاء صلاحية.
5. روابط ملفات Storage عامة ودائمة (قرار منتج معلَّق).
   > ⚠️ صُحِّح 2026-09-06: كان مكتوباً «صور الفواتير» وهي **لا تُرفع إطلاقاً** —
   > `uploadReceiptImage` بلا مستدعٍ واحد، ولا حقل صورة في `Expense`. المقصود
   > فعلاً: إثباتات وتواقيع «سلفتنا» (`users/{uid}/silftna/…`).
6. `xlsx` فيها ثغرة عالية بلا إصلاح منشور، وتقرأ ملفاً يرفعه المستخدم.
7. 🆕 **«حذف حساب العائلة» يعد بما لا ينفّذه** (رُصد 2026-09-07 من لقطة المالك).
   النافذة تقول «سيتم حذف حساب العائلة **وإزالة جميع الأعضاء**»، وهي تنادي
   `leaveHousehold` التي **ترمي** لو كان المالك ومعه أعضاء:
   «قم بنقل الملكية أو إزالتهم أولاً» — **ونقل الملكية غير موجود في التطبيق
   أصلاً**. فالزر معطَّل عملياً لكل مالك في عائلة فيها غيره، ويعمل فقط لمن كان
   وحده (وحينها يعيد بيانات العائلة كلها إلى حسابه — وهو ما لا يقوله النصّ).
   ⇒ ثلاثة طرق عُرضت على صاحب المشروع: تصحيح النصّ فقط · إضافة نقل ملكية
   (المنطق جاهز في `account-erasure.ts` ⇐ `pickHeir`) · جعل الحذف يحلّ العائلة
   ويعيد لكل عضو بياناته. **لم يقرّر بعد — لا تبدأه بلا إذنه.**
8. 🆕 **أيقونات الأدوات أثقل من حاجتها ٧×** (قيست 2026-09-08): ٧٤٨ ك.ب للثماني،
   أبعادها ٣٧٥×٣٧٥ بكسل وتُعرض بـ٥٦ (أثقلها `silftna` ١٥٦ · `currency` ١٤٦).
   تصغيرها يقلّص ما ينزل على كل مستخدم **بكل نشر**. عُرض ولم يُقرَّر.
   ⚠️ ولا تَعِد برقم قبل قياسه بعد التصغير فعلاً.
9. 🆕 **`manual-expense-form` يغلق في `onMutate`** — نفس نمط فخّ ٥ أدناه، ولم يُمسّ
   عمداً (خارج نطاق ما طُلب، وأثره ضيّق: النافذة لا تُفتح إلا بعد تفريغ صوتي ناجح
   أي مع وجود شبكة). مرشّح مراجعة. عُرض 2026-09-08 ولم يُقرَّر.
10. ~~سبع لقطات شاشة يتيمة ٣٫١ م.ب~~ ⇒ **نُقلت 2026-09-09 بقراره** — انظر «✅ أُصلح» أدناه.

### ⛔ ستة فخاخ — تبدو إصلاحات سهلة وهي تكسر
1. **مفاتيح `localStorage` غير مرتبطة بالحساب** (قفل التطبيق · الترحيب · الأوسمة ·
   ٤ أنظمة تذكير). تغيير أسمائها **يمسح حالة كل المستخدمين الحاليين** — يعود معالج
   الترحيب للجميع وتتكرّر إشعارات الإنجازات. يحتاج **ترحيلاً** لا إعادة تسمية.
   > استثناء: `tadbeer-hh:<uid>` (آخر معرّف عائلة، أُضيف 2026-09-05) **مربوط بالحساب**
   > ومجرّد ذاكرة تسريع تُصحَّح نفسها — خارج هذا الفخّ، ويجوز مسحه بلا أثر.
2. **متغيّرات `firebase-admin` في Vercel فقط.** أي مسار يتحقق من الهوية يفشل دائماً
   على `npm run dev`. **لا تستنتج من رفض محلي أن المصادقة مكسورة** — قارن بمسار
   إداري قائم أولاً. (٥٠٣ = إعداد ناقص · ٤٠١ = رمز فعلاً غير صالح.)
3. **مهمة التذكير اليومي سليمة — قيست ولم تُقطع.** ١٣٠ مشتركاً في ١٨ ثانية، وثلاث
   تشغيلات يومية تكتمل بـ٢٠٠. لا «تُصلح» عطلاً غير موجود؛ الخطر نموّ خطّي لاحق فقط.
4. **نافذة داخل قسم مطويّ = نافذة غير موجودة.** محتوى `AccordionContent` المغلق
   يُزيله Radix من الشجرة، فأي `<Dialog>` مكتوب داخله **لا يُركَّب أصلاً**: زرٌّ
   خارج الطيّ يرفع الحالة ولا يحدث شيء، ثم تنفتح النافذة فجأةً حين يُفتح القسم.
   حدث فعلاً بـ«شاركنا رأيك» (2026-09-05 → أُصلح 09-06). **كل نافذة يستدعيها زرّ
   خارج الطيّ تُكتب على مستوى الصفحة** — كما في `receipts/page.tsx`.
5. **مغادرة الشاشة في `onMutate` تقتل `onError`.** توابع `useMutation` مرتبطة
   بالمكوّن الحيّ، فأي `router.push` أو `setOpen(false)` داخل `onMutate` يُفكِّك
   المكوّن ⇒ **لا `onError` ولا `onSuccess` ولا `onSettled`** ⇒ **يضيع التراجع عن
   التحديث المتفائل** فيبقى مصروف وهمي معروضاً لم يُحفظ، بلا رسالة خطأ.
   أُمسك قبل الشحن 2026-09-08 في `add-expense`: الخروج المبكر صار **بلا إنترنت
   فقط** (لا فشل هناك يُتراجَع عنه — Firestore يصفّ الكتابة)، والمسار المتصل ينتظر
   تأكيد الخادم كما كان. ⚠️ `manual-expense-form` يغلق في `onMutate` ويحمل النمط
   نفسه — لم يُمسّ، وهو مرشّح مراجعة لاحقة.
6. **ملف موجود ≠ شاشة يراها المستخدم.** `grep` يجد الملف على القرص فتظنّه جزءاً من
   المنتج، وهو مرفوع من المسار منذ شهور. حدث فعلاً 2026-09-09: جلسة وصفت **صفحة
   الهبوط** ضمن «الجاهز للتوسّع العربي»، وهي مرفوعة من المسار منذ 2026-07-11
   (`886fc65`) — الزائر يصل `/signup` مباشرةً — وصاحب المشروع هو من صحّح.
   الملف الميت حُذف (`src/app/landing/page.tsx`) ومعه أربعة تعليقات كانت تحيل إليه
   (`(main)/layout.tsx` · `sitemap.ts` · `lock-screen.tsx` · `login/page.tsx`).
   ⛔ **قبل وصف أي شاشة بأنها موجودة، تتبّع مسارها فعلاً** — من يصل إليها ومن أين.
   الشاشة الأولى للزائر هي `/signup` وحدها. راجع `(main)/layout.tsx` عند الشكّ.

📄 التفاصيل الكاملة والأدلة: ملف الذاكرة `tadbeer_audit_2026-09-05.md`.

## 🗣️ أسلوب التعامل مع صاحب المشروع
- **غير مبرمج.** لا تفاصيل تقنية بالكلام معه — بس: شنو صار، وشنو مطلوب منه بالضبط (إن وجد).
- **افعل بنفسك قبل ما تطلب منه.** لا تحوّل له خطوة إلا بعد ما تستنزف كل طريقة تسويها أنت. اطلب منه فقط شي يستحيل عليك تنفيذه (فعل مادي، أو شي يخص حسابه الشخصي).
- **القرارات استثناء.** خيار يخص ذوقه أو منتجه (تسمية، أولوية، قبول/رفض ميزة) ليس "خطوة تقنية" — هذا سؤاله دائماً، ولو كان صغيراً.
- التفاصيل التقنية الكاملة تُكتب بالكود أو التعليقات للجلسات القادمة — لا تُقال له بالمحادثة.

## ⛔ لغة التطبيق — عربية بسيطة، والعامية في موضع واحد فقط

**كل نصّ يراه المستخدم بالعربية البسيطة الواضحة لمختلف الشعوب العربية** — لا عامية عراقية، ولا فصحى متكلَّفة. أُكِّدت 2026-09-05 بعد أن تسرّبت خمسة مواضع عامية («وراح تُراجَع» · «شنو نناديك» ×2 · «وشوف تدبير يشتغل» · «منها تشتغل التنبيهات» — والأخيرة فيها خطأ تذكير/تأنيث أيضاً).

**الاستثناء الوحيد المقصود — لا يُمسّ:** شخصية **«كرومي»** (`appTone: 'colloquial'`) في تدفقات الذكاء. عامية عراقية محترمة ومشجِّعة، **يختارها المستخدم بنفسه** من الإعدادات مقابل «أستاذ حريص» (فصحى)، وتشمل نصائح المدرّب. ⛔ لا تُلطَّف ولا تُحذف — قرار صريح لصاحب المشروع.

**فحص سريع قبل أي نصّ جديد:**

```bash
grep -rnE "راح ي|راح ت|راح ن|شنو|شلون|هسه|كلش|خوش|ماكو|ليش|يشتغل|تشتغل" src/app src/components
```

أي نتيجة **خارج** `src/ai/flows/` مخالفة.

## ⛔ قوانين المحاذاة RTL — إلزامية بكل ميزة جديدة
- **أسلوب واحد للاتجاه، لا اثنان.** `dir="rtl"` على `<html>` (موجود، `layout.tsx:102`) هو المصدر الوحيد. ممنوع أي منطق JS يقلب اتجاهاً يدوياً فوقه — يلغي بعضه ويعطي نتائج متضاربة حسب حالة التشغيل.
- **ممنوع** `ml- mr- pl- pr- left- right- text-left text-right` بأي ملف جديد أو مُعدَّل. البديل: `ms- me- ps- pe- start- end- text-start text-end` (منطقية، تتبع الاتجاه تلقائياً).
- **شريط أي تقدّم (ميزانية، تقسيط، تحميل) يمتلئ من اليمين** — عنصر التعبئة `start-0` لا `left-0`.
- **كل رقم مع وحدة** (مبلغ، نسبة، تاريخ) **يُلفّ بـ`<bdi>`** — يمنع انقلاب «5000 د.ع» إلى «د.ع 5000».
- **الأيقونات الاتجاهية تنعكس كالمرآة** (سهم رجوع، تالي/سابق، إرسال) — `rtl:-scale-x-100` أو مكافئها.
- ⚠️ فحص سريع قبل أي PR: `grep -rE "\b(ml|mr|pl|pr)-[0-9]|text-(left|right)" src` يجب أن يرجّع صفراً على الملفات المُعدَّلة.

## 🔍 أمر الجرد السريع (شغّله قبل أي نقاش عن الميزات)

```powershell
$s="src"
Get-ChildItem "$s\app" -Recurse -Filter "page.tsx" | % { $_.DirectoryName.Replace("$s\app","").Replace("\","/") }   # الشاشات
Get-ChildItem "$s\app\api" -Recurse -Filter "route.ts" | % { $_.DirectoryName.Replace("$s\app\api","") }            # APIs
Get-ChildItem "$s\ai\flows" -Filter "*.ts" | % { $_.BaseName }                                                       # تدفقات AI
Get-ChildItem "$s\hooks","$s\lib","$s\services" -Filter "*.ts*" | % { $_.BaseName }                                  # المنطق
```

---

## الهوية التقنية
Next.js 15.2.8 (App Router) · React 18 · TypeScript صارم (`ignoreBuildErrors:true`) · PWA (`@ducanh2912/next-pwa`, NetworkFirst) · Firebase (Auth+Firestore+Storage) · firebase-admin **مثبّت على ^12 عمداً** (jose@6 يكسره) · Genkit + Gemini 2.5-flash · Web Push + 4 Vercel Cron · RTL عربي · v1.3.0 (package.json).

## نماذج البيانات (`src/types/index.ts` — 15 كياناً)
Expense · Goal · Income · UserSettings · Category · Household · HouseholdMember · Debt · InstallmentPlan · WeddingPlan · Silftna · EarnedBadge · RecurringPayment · UserBudgetSettings · NotificationSettings.

## ⚠️ المعمارية الجوهرية — المسار المزدوج (Dual-Path)
كل بيانات قد تكون شخصية أو عائلية حسب `householdId`:
```
users/{uid}/...        ← شخصي
households/{hhId}/...   ← مشترك (عائلة)
```
- كل دالة في `services/firestore.ts` تقبل `householdId?` لتقرّر المسار.
- `getUserSettings` يدمج: المشترك (ميزانية/فئات/دخل) + الشخصي (نبرة/إشعارات/عملة).
- `_moveSubcollection` (انسخ ثم احذف) = قلب الانضمام/المغادرة، يمنع فقدان البيانات.
- كل مستند يحفظ `uid` منشئه (ضروري لاسترجاع البيانات عند مغادرة العائلة).

## إدارة الحالة
- React Query: `staleTime:5د` · `gcTime:30د` · `refetchOnWindowFocus:false` · `retry:1`.
- `AppDataProvider` (`hooks/use-app-data.tsx`) يجمع كل شيء في context واحد.
- ⚠️ queryKey المصاريف بالضبط: `['expenses', uid, householdId, 'recent']` — أماكن أخرى تعتمد عليه.
- المصاريف تُحمَّل لآخر **6 أشهر فقط** (أداء). لكامل المصاريف استخدم `getExpenses` مباشرة (كما في شاشة expenses).
- ترتيب المزوّدين: `Theme → Query → Auth → SwUpdater` (`components/providers.tsx`).

## المسارات (14 شاشة)
**محمية (10):** `/` · expenses · stats · report · planner · receipts · achievements · settings · add-expense · tools/* — الحماية في `(main)/layout.tsx`.
**عامة (4):** landing · login · signup · privacy.
**شريط سفلي (5):** الرئيسية · المصاريف · الإحصائيات · الأدوات · الأهداف (`components/layout/page-navigation.tsx`).

## الأدوات الثماني (`src/app/(main)/tools/`)
| الأداة | التخزين |
|---|---|
| currency (تحويل عملات + سعر سوق عراقي) | API + LocalStorage |
| debts (دفتر ديون) | Firestore |
| habit-cost (تكلفة العادات) | محلي |
| installment (أقساطي — يدعم القروض السابقة) | Firestore |
| safarati (سفراتي — ميزانية سفر) | Firestore (`users/{uid}/trips`) |
| silftna (جمعية دوّارة) | Firestore + Storage |
| wedding (حاسبة زواج) | Firestore |
| worth-it (هل يستحق؟) | محلي |

> ⚠️ صُحِّح 2026-09-05: كان مكتوباً «الأدوات السبع» وسفراتي غائبة عن الجدول، وهي
> مبنيّة ومدفوعة وموجودة في `tools/page.tsx` فعلاً. مستنداتها شخصية دائماً
> (`users/{uid}/trips`) — أُضيفت إلى مسار حذف الحساب بنفس التاريخ بعد أن كانت تبقى بعده.
>
> ⛔ **«سفراتي» مؤخَّرة عمداً — لا تُروَّج.** صاحب المشروع وضعها **آخر** الأدوات ولم
> يجعلها ضمن أوائل الخيارات في الشاشة الرئيسية **قصداً**، كي لا ينتبه لها المستخدمون
> قبل مراجعتها. فلا تُقدَّم في الترتيب، ولا تُضاف إلى بطاقة الأدوات بالرئيسية، ولا
> تُذكَر في تسويق أو جولة تعريفية — إلى أن يطلب هو ذلك صراحةً. (أُكِّد 2026-09-05
> بعد أن وصفها فحصٌ سابق بأنها «معروضة» بلا ذكر هذا القيد.)
> **وهي تنتظر مراجعة لاحقة** لم تتم بعد.

منطق الحساب: `lib/tools-calc.ts` · `lib/silftna.ts` · `lib/billing-utils.ts` · أسعار الصرف: `hooks/use-exchange-rates.ts`.

## طبقة الذكاء (9 تدفقات في `src/ai/flows/` · 10 APIs في `src/app/api/`)
استخراج إيصال · اتجاهات شهرية · أنماط إنفاق · محادثة (مستشار الجيب) · مدرب · مخطط · تسجيل نصي · تسجيل صوتي · ملخص إحصائي (**هذا بلا AI — حساب نقي بـ date-fns**).
- كلها `gemini-2.5-flash` (`ai/genkit.ts`)، `maxDuration:60`.
- الأخطاء تُرجع `status 200` مع `{ok:false, error}` (للتوافقية).
- `thinkingBudget:0` في الكل ما عدا التسجيل الصوتي (512).
- إشعارات: subscribe/send/month-end عبر web-push + VAPID + CRON_SECRET.

## أنظمة مساعدة
- **أوسمة (8، `lib/badges.ts`):** first_expense · week_logger · zero_day · family_leader · month_saver · big_saver · social_pro · report_viewer.
- **إشعارات ذكية (5، `hooks/use-smart-notifications.tsx`):** تذكير يومي · ملخص شهري · تنبيه ميزانية 90% · تذكير فواتير · سلسلة صفرية. منع التكرار عبر localStorage.
- **توطين عراقي (`lib/arabic-date.ts`):** كانون/شباط/آذار... + 12 فئة افتراضية (`lib/constants.ts`).
- معالجة صارمة UTC vs محلي في السلاسل (`use-logging-streak` · `use-zero-streak`).
- كشف التكرار المركزي: `lib/duplicate-check.ts`. تحويل صوت→WAV: `lib/audio-to-wav.ts`.

## أوامر التطوير
`npm run dev` · `npm run build` · `npm run lint` · `npm run typecheck` · `npm run genkit:dev`.

## نقاط تستحق الانتباه (مُحدَّثة 2026-08-22 — الثلاث الأولى من 06-25 كانت قديمة وصُحِّحت)
- `public/worker-*.js` غير متتبَّع في git (الاسم يتغيّر كل بناء PWA) — SW قديم متبقٍّ، لا يؤثر على التشغيل.
- ⚠️ **صُحِّح 2026-09-05:** `xlsx` (`^0.18.5`) **مستخدمة فعلاً** — لا «غير مستخدمة إطلاقاً» كما كان مكتوباً هنا. تُستورَد ديناميكياً مرّتين في `settings/page.tsx` وتُشغّل تصدير المصاريف إلى Excel واستيرادها منه. وفيها ثغرتان معلنتان (تلويث Prototype + ReDoS) **بلا إصلاح منشور**، وهي تقرأ ملفاً يرفعه المستخدم — أي أنها سطح هجوم حقيقي لا تبعية خاملة. `pptxgenjs` لم تعد بالتبعيات أصلاً — احذف أي إشارة سابقة لها.
- v1.2.0 نُشر رسمياً على Google Play (versionCode 3) بتاريخ 2026-08-22 — راجع `docs/releases/v1.2.0.md`.
- ⚠️ `package.json` صار **1.3.0** بعدها ويظهر في شاشة «حول تدبير» — أي أن الإصدار المنشور على Play أقدم من إصدار الويب. تحقّق من الاثنين قبل أي كلام عن «الإصدار الحالي».

## قواعد مقفلة (لا تعدّلها إلا بطلب صريح)
أدوات: currency · worth-it · installment · habit-cost — نسخ نهائية مقفلة. راجع ملفات الذاكرة قبل المساس بها.

> **currency:** فُتحت مرّة واحدة بإذن صريح (2026-09-05) لإصلاح عيب أسعار ناقصة كانت
> تُحسب بسعر ١ فتُظهر رقماً كاذباً — ثم **أُعيد إغلاقها**. الاستثناء لا يفتحها لغيره.

> **سفراتي:** ليست ضمن المقفلة، لكنها **مؤخَّرة عمداً ولا تُروَّج** — راجع قسم الأدوات أعلاه.
